# Backend PostgreSQL Connection URL (Provided by Render PostgreSQL or local instance)
DATABASE_URL=postgres://username:password@hostname:5432/TaskManagementDB

# Frontend Backend API URL (Used by Next.js to call the deployed Render Web Service)
NEXT_PUBLIC_API_URL=https://your-backend-service.onrender.com
